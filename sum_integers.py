# Challenge: Sum two ingegers
# Create a function called sum_integers that accepts two integers and returns their sum.

# Tests:
assert sum_integers(10, 1) == 11
assert sum_integers(400, 1000) == 1400
assert sum_integers(0, 0) == 0
